import { SpaceMember } from '@/entities/member'

interface Props {
  members: SpaceMember
}

const SpaceMemberTable = ({ members }: Props) => {
  return <div>SpaceMemberTable</div>
}
export default SpaceMemberTable
